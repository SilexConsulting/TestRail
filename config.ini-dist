; TestRail location, username and password
url = 'https://example.testrail.com/'
username = 'example@example.com'
password = ''

; ID of source project for TestRail scripts (use the listprojects.php script to discover project IDs)
source = 99

; ID of destination project for TestRail scripts; will be made to mirror the source project
destination = 100

; path to log file
log = ''
